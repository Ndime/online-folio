{
  "name": "terencendime",
  "homepage": ".",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.22.0",
    "node-sass": "^4.14.1",
    "react": "^18.0.0",
    "react-app-polyfill": "^1.0.6",
    "react-dom": "^18.0.0",
    "react-eva-icons": "0.0.8",
    "react-lazyload": "^2.6.9",
    "react-router-dom": "^6.4.0",
    "react-scripts": "^4.0.3",
    "react-ticker": "^1.3.2",
    "rimraf": "^5.0.5"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && git add . && git commit -n --allow-empty-message",
    "build-actions": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "pre-commit": [
    "build"
  ],
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "pre-commit": "^1.2.2"
  }
}